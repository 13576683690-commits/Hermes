# Core agent / LLM framework
langchain-core>=0.3.0
langchain-deepseek>=0.1.2
langchain-huggingface>=0.1.2
langgraph>=0.2.0

# OpenAI-compatible client (used for DeepSeek, OpenAI and local vLLM)
openai>=1.30.0
backoff>=2.2.1
httpx>=0.27.0

# Embeddings (optional — only needed if you enable memory-backed prerequisite retrieval)
sentence-transformers>=2.6.0

# Utilities
loguru>=0.7.0
easydict>=1.13
pydantic>=2.5.0
pytz>=2024.1
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
termcolor>=2.4.0

# ML / inference stack (required by prover/workers and utils/models for vLLM-served models)
# Comment these out if you only call hosted APIs (DeepSeek/OpenAI).
torch>=2.2.0
transformers>=4.43.0
vllm>=0.5.0
